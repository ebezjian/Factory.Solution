@{
  Layout = "_Layout";
}

@using Factory.Models;
@model List<Factory.Models.Engineer>;

<h1>Engineer Directory</h1>

@* @if (@Model.Count == 0)
{
  <h3>This Factory currenly has no Engineers....who's running the place?</h3>
} *@

@foreach (Engineer engineer in Model)
{
  <ul>
    <li>@Html.ActionLink($"{engineer.EngineerName} ", "Details", new { id = engineer.EngineerId }) </li>
  </ul>
}

<p>@Html.ActionLink("Add a new Engineer", "Create")</p>